############
Functions:
    create_files -> create files with random numbers in them
    sort_files -> sort those files in parallel
    main -> first creates files then sorts them
############
Run:
    python thread.py
    # No Additional Files Needed
############
Files:
    # After Running:
    f<i> -> randomly create numbers
    sorted f<i> -> sorted version of f<i> files
############
Requirements:
    <random> package
############